---
interface Props {
  class?: string;
}
const { class: className = "" } = Astro.props;
---
<div class={`prose dark:prose-invert max-w-2xl mx-auto w-full px-6 py-12 ${className}`}>
  <slot />
</div>
